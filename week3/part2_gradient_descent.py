balance = [0.65, 0.80, 0.80, 0.90]
clean = [1, 1, 0, 1]
# Train on sensing 0: input = 0.65, goal = 1
weight = 0.5 # starting weight
# TODO: write gradient_descent(input, goal, weight, iterations)
# Print error and prediction at each step
# The loop: pred, error, delta, weight_delta, weight update