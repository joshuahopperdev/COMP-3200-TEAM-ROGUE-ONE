# week3/part1_error.py
balance = [0.65, 0.80, 0.80, 0.90]
clean = [1, 1, 0, 1]
weight = 0.5
# TODO: write squared_error(prediction, goal) -- pure Python first.
# Then compute predictions and errors for all four sensings.